<UserControl x:Class="WorkflowCore.WpfDemo.Views.MethodDescriptionEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppPageBrush}"
             Foreground="{DynamicResource AppTextBrush}">
    <UserControl.Resources>
        <Style x:Key="DescriptionCommandButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Height" Value="30" />
            <Setter Property="MinHeight" Value="0" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Padding" Value="7,2" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
        </Style>
        <Style x:Key="DescriptionFormatButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Margin" Value="1,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
            <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="38" />
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
            <RowDefinition Height="26" />
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource AppPanelBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="0,0,0,1">
            <TextBlock Text="Edit method description" Margin="12,0" FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center" />
        </Border>

        <Border Grid.Row="1" Background="{DynamicResource AppPanelBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel Margin="8,5">
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Click="CloseOnClick" ToolTip="Save changes and return to the method">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,5,0" />
                        <TextBlock Text="Close" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource DescriptionCommandButtonStyle}"
                        Command="{x:Static ApplicationCommands.Undo}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Undo">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7A7;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,5,0" />
                        <TextBlock Text="Undo" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Click="LoadDocumentOnClick" ToolTip="Load description from file">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8E5;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,5,0" />
                        <TextBlock Text="Load from file" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Click="SaveDocumentOnClick" ToolTip="Save description to file">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,5,0" />
                        <TextBlock Text="Save to file" />
                    </StackPanel>
                </Button>
                <Separator Margin="6,1" />
                <ToggleButton Style="{StaticResource DescriptionFormatButtonStyle}" Content="B" FontWeight="Bold"
                              Command="{x:Static EditingCommands.ToggleBold}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Bold" />
                <ToggleButton Style="{StaticResource DescriptionFormatButtonStyle}" Content="I" FontStyle="Italic"
                              Command="{x:Static EditingCommands.ToggleItalic}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Italic" />
                <ToggleButton Style="{StaticResource DescriptionFormatButtonStyle}"
                              Command="{x:Static EditingCommands.ToggleUnderline}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Underline">
                    <TextBlock Text="U" TextDecorations="Underline" />
                </ToggleButton>
                <ComboBox x:Name="FontSizeBox" Width="60" Height="28" Margin="4,0,2,0" IsEditable="True" SelectedIndex="3"
                          SelectionChanged="FontSizeOnSelectionChanged" LostKeyboardFocus="FontSizeOnLostKeyboardFocus" ToolTip="Font size">
                    <ComboBoxItem Content="10" /><ComboBoxItem Content="12" /><ComboBoxItem Content="13" /><ComboBoxItem Content="14" />
                    <ComboBoxItem Content="16" /><ComboBoxItem Content="18" /><ComboBoxItem Content="24" /><ComboBoxItem Content="32" />
                </ComboBox>
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Padding="5,2" Content="&#xE8E4;" FontFamily="Segoe MDL2 Assets"
                        Command="{x:Static EditingCommands.AlignLeft}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Align left" />
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Padding="5,2" Content="&#xE8E3;" FontFamily="Segoe MDL2 Assets"
                        Command="{x:Static EditingCommands.AlignCenter}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Align center" />
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Padding="5,2" Content="&#xE8E2;" FontFamily="Segoe MDL2 Assets"
                        Command="{x:Static EditingCommands.AlignRight}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Align right" />
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Padding="5,2" Content="&#xE8FD;" FontFamily="Segoe MDL2 Assets"
                        Command="{x:Static EditingCommands.ToggleBullets}" CommandTarget="{Binding ElementName=Editor}" ToolTip="Bulleted list" />
                <Button Style="{StaticResource DescriptionCommandButtonStyle}" Padding="5,2" Content="&#xEB9F;" FontFamily="Segoe MDL2 Assets"
                        Click="InsertImageOnClick" ToolTip="Insert image" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="2" Background="{DynamicResource AppPageBrush}" ClipToBounds="True">
            <Border Margin="36,16" HorizontalAlignment="Center" Width="820" MaxWidth="820"
                    Background="White" BorderBrush="#FF777777" BorderThickness="1">
                <RichTextBox x:Name="Editor" BorderThickness="0" Padding="0" AcceptsTab="True"
                             VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                             Background="White" Foreground="Black" CaretBrush="Black" SelectionBrush="#FFB9DDF7"
                             SpellCheck.IsEnabled="True" TextChanged="EditorOnTextChanged" />
            </Border>
        </Grid>

        <Border Grid.Row="3" Background="{DynamicResource AppPanelBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock x:Name="StatusText" Margin="10,0" Foreground="{DynamicResource AppMutedTextBrush}" VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>
