<Window x:Class="WorkflowCore.WpfDemo.Views.ActionLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Action Log"
        Width="940"
        Height="560"
        MinWidth="680"
        MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppPageBrush}"
        Foreground="{DynamicResource AppTextBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/WorkflowEditorResources.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="42" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Background="{DynamicResource AppPanelBrush}">
            <TextBlock Text="Action Log" FontSize="16" FontWeight="SemiBold"
                       Margin="12,0" VerticalAlignment="Center" />
            <Button DockPanel.Dock="Right" Content="Clear" Margin="5" Padding="12,3"
                    Click="ClearOnClick" />
            <TextBlock DockPanel.Dock="Right"
                       Text="{Binding Events.Count, StringFormat={}{0} records}"
                       Margin="8,0" Foreground="{DynamicResource AppMutedTextBrush}"
                       VerticalAlignment="Center" />
        </DockPanel>
        <DataGrid Grid.Row="1" ItemsSource="{Binding Events}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="All"
                  RowHeight="28" ColumnHeaderHeight="30" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Time" Binding="{Binding Time}" Width="165" />
                <DataGridTemplateColumn Header="Action" Width="1.3*" MinWidth="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <ContentControl Width="20" Height="20" Margin="0,0,6,0"
                                                Content="{Binding ActionTemplate}"
                                                ContentTemplate="{StaticResource ActionIconTemplate}" />
                                <TextBlock Text="{Binding ActionName}" VerticalAlignment="Center" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="2*" MinWidth="220" />
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="1.2*" MinWidth="150" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
