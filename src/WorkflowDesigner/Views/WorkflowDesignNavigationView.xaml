<UserControl x:Class="WorkflowCore.WpfDemo.Views.WorkflowDesignNavigationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent"
             Foreground="{DynamicResource AppTextBrush}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/WorkflowEditorResources.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="WorkflowNavigationButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="46" />
                <Setter Property="Margin" Value="1" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Foreground" Value="{DynamicResource AppTextBrush}" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="NavigationItemChrome"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="0">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="NavigationItemChrome"
                                            Property="Background"
                                            Value="{DynamicResource AppHoverBrush}" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="NavigationItemChrome"
                                            Property="Background"
                                            Value="{DynamicResource AppButtonPressedBrush}" />
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.4" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="WorkflowNavigationItemButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource WorkflowNavigationButtonStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                        <Setter Property="Background" Value="{DynamicResource AppSelectedBrush}" />
                        <Setter Property="Foreground" Value="{DynamicResource AppAccentBrush}" />
                        <Setter Property="FontWeight" Value="SemiBold" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Style x:Key="WorkflowNavigationListBoxItemStyle" TargetType="ListBoxItem">
                <Setter Property="Height" Value="48" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Margin" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border Background="Transparent">
                                <ContentPresenter HorizontalAlignment="Stretch"
                                                  VerticalAlignment="Center" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border Margin="0,2"
            Background="{DynamicResource AppPageBrush}"
            BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Button Grid.Row="0"
                    Command="{Binding ToggleExplorerCommand}"
                    Style="{StaticResource WorkflowNavigationButtonStyle}"
                    ToolTip="{Binding NavigationToggleToolTip}">
                <Path Width="22"
                      Height="18"
                      Data="M2,3 H20 M2,9 H20 M2,15 H20"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.6"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center" />
            </Button>

            <ListBox Grid.Row="1"
                     Margin="0,4,0,0"
                     ItemsSource="{Binding HamburgerMenuItems}"
                     SelectedItem="{Binding SelectedHamburgerMenuItem, Mode=TwoWay}"
                     ItemContainerStyle="{StaticResource WorkflowNavigationListBoxItemStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Command="{Binding DataContext.SelectHamburgerMenuCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                CommandParameter="{Binding}"
                                Style="{StaticResource WorkflowNavigationItemButtonStyle}"
                                ToolTip="{Binding Title}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="48" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <ContentControl Content="{Binding DocumentKind}"
                                                ContentTemplate="{StaticResource WorkflowDocumentIconTemplate}"
                                                Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                Width="24"
                                                Height="24"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center" />
                                <TextBlock Grid.Column="1"
                                           Margin="4,0,10,0"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                           FontSize="15"
                                           Text="{Binding Title}"
                                           TextTrimming="CharacterEllipsis"
                                           Visibility="{Binding DataContext.IsExplorerExpanded, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Border Grid.ColumnSpan="2"
                                        Width="3"
                                        Margin="0,6,0,6"
                                        HorizontalAlignment="Left"
                                        Background="{DynamicResource AppAccentBrush}"
                                        CornerRadius="1"
                                        IsHitTestVisible="False"
                                        Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <Border Grid.Row="2"
                    Margin="0"
                    Padding="0,4,0,1"
                    BorderBrush="{DynamicResource AppBorderBrush}"
                    BorderThickness="0,1,0,0">
                <Button Command="{Binding NewMethodCommand}"
                        Style="{StaticResource WorkflowNavigationButtonStyle}"
                        ToolTip="Create...">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Path Width="20"
                              Height="20"
                              Data="M10,2 V18 M2,10 H18"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.6"
                              StrokeStartLineCap="Round"
                              StrokeEndLineCap="Round"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1"
                                   Margin="4,0,10,0"
                                   VerticalAlignment="Center"
                                   Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                   FontSize="16"
                                   Text="Create..."
                                   TextTrimming="CharacterEllipsis"
                                   Visibility="{Binding IsExplorerExpanded, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </Grid>
                </Button>
            </Border>
        </Grid>
    </Border>
</UserControl>
